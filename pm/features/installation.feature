# language: zh-TW
Feature: 安裝與排程任務建立
  作為 第一次取得標案搜尋工具的使用者
  我希望 一次安裝就完成桌面捷徑、資料目錄與每日排程設定
  以便 我不必額外手動設定 Windows Task Scheduler 也能享受每日自動更新

  Background:
    Given 使用者具備在 Windows 上執行安裝程式的權限
    And 使用者尚未安裝過此標案搜尋工具

  # ----------------------------------------------------------
  # Happy path
  # ----------------------------------------------------------
  Scenario: 全新安裝完成，建立資料根目錄、捷徑與排程任務
    When 使用者執行安裝檔並完成安裝精靈
    Then 系統應建立資料根目錄 "%LocalAppData%/TenderSearch/data/"
    And 系統應在桌面建立桌面捷徑
    And 系統應在開始功能表建立捷徑
    And 系統應建立 Windows 排程任務，預設於每日 17:00 觸發爬蟲
    And 排程任務即使在桌面程式未開啟時也能執行
    And 安裝精靈最後一頁應顯示安裝結果摘要

  Scenario: 安裝完成後排程可實際執行
    Given 使用者已成功安裝
    And 系統時間為 "2026-05-08 17:00"
    When 排程觸發時間到達
    Then 系統應自動執行每日爬蟲
    And 即使使用者尚未開啟桌面程式，仍應產生當日 "tenders.json"

  # ----------------------------------------------------------
  # 重複安裝
  # ----------------------------------------------------------
  Scenario: 重新安裝時不可覆蓋已存在的資料目錄
    Given 資料根目錄 "%LocalAppData%/TenderSearch/data/" 已存在且包含過去資料
    When 使用者再次執行安裝檔
    Then 系統不應刪除或覆蓋既有資料目錄內容
    And 既有日期資料夾與其中的 JSON 檔案應完整保留

  Scenario: 重新安裝時若排程任務已存在不應重複建立
    Given 既有 Windows 排程任務已存在
    When 使用者再次執行安裝檔
    Then 系統不應額外建立第二個重複的排程任務
    And 既有排程任務的時間設定應被保留或被使用者明確確認後才覆寫

  # ----------------------------------------------------------
  # 錯誤情境
  # ----------------------------------------------------------
  Scenario: 沒有權限建立排程任務
    Given 使用者執行安裝程式時未獲得建立排程任務所需的權限
    When 安裝程式嘗試建立 Windows 排程任務
    Then 安裝程式應提示無法建立排程任務的錯誤
    And 安裝程式應仍完成桌面捷徑與資料目錄的建立
    And 安裝結果摘要應明確標示排程任務「未建立」並提示使用者後續處理

  Scenario: 資料根目錄無法建立
    Given 使用者帳戶對 "%LocalAppData%/TenderSearch/" 路徑無寫入權限
    When 安裝程式嘗試建立資料根目錄
    Then 安裝程式應顯示錯誤訊息並中止安裝
    And 不應建立殘缺的捷徑或排程任務

  Scenario: 安裝過程中使用者取消
    When 使用者於安裝精靈中點擊「取消」
    Then 系統不應在桌面留下捷徑
    And 系統不應建立 Windows 排程任務
    And 系統不應留下不完整的資料根目錄

  # ----------------------------------------------------------
  # 解除安裝（基本期待，需求第 7 節隱含）
  # ----------------------------------------------------------
  Scenario: 解除安裝時保留使用者既有資料
    Given 使用者已使用此工具產生數日的標案資料
    When 使用者執行解除安裝
    Then 桌面捷徑與開始功能表捷徑應被移除
    And Windows 排程任務應被移除
    And 資料根目錄 "%LocalAppData%/TenderSearch/data/" 中的歷史資料夾應仍保留，避免使用者誤失
