<Window x:Class="Tender.Desktop.Views.ErrorSummaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="錯誤摘要"
        Height="500" Width="720"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource BgMainBrush}"
        FontFamily="Microsoft JhengHei UI">
    <DockPanel>
        <Border DockPanel.Dock="Top" Padding="18,14"
                Background="{StaticResource ErrorLightBrush}"
                BorderBrush="{StaticResource ErrorBrush}"
                BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock x:Name="TitleText"
                           FontSize="16" FontWeight="SemiBold"
                           Foreground="{StaticResource ErrorDarkBrush}" />
                <TextBlock x:Name="ErrorMessageText" Margin="0,5,0,0"
                           Foreground="{StaticResource ErrorDarkBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Padding="14"
                Background="{StaticResource BgCardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="OpenLogButton" Content="📁 開啟 errors.log" Padding="11,5" Margin="0,0,8,0" Click="OpenLog_Click" />
                <Button Content="關閉" Padding="22,5" IsDefault="True" Click="Close_Click"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Border>

        <Border Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="LogContent"
                         IsReadOnly="True"
                         FontFamily="Consolas, Cascadia Mono"
                         FontSize="12"
                         Background="#2A2218"
                         Foreground="#E5DCC8"
                         Padding="11"
                         BorderBrush="{StaticResource BorderStrongBrush}"
                         BorderThickness="1"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         MinHeight="280" />
            </ScrollViewer>
        </Border>
    </DockPanel>
</Window>
